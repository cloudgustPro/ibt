mon 20 july class
- In today's class we learnt sort and search algorithm and there are a number of sorting and serching algorithm such as linear, constant, binary, quadratic

Binary Search 
The function takes a sorted list and a target value as input and returns the index of the target value if found, or -1 if not found. The algorithm works by repeatedly dividing the search interval in half, comparing the middle element with the target value, and adjusting the search boundaries accordingly.